Nicholas Cameron @nick_r_cameron

questions:

- does '?' work for both Option and Result?
  - yes.

  - QQQ is it generic?

- what is FnOnce ? (in Option map impl)

- ``where F : FnOnce(T) -> Option<U>``
  - can that be inlined into the main signature?
